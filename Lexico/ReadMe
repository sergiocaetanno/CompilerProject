O analisador léxico desenvolvido lê um arquivo de texto como entrada (arquivo_entrada) e retorna os tokens associados a cada um de seus lexemas;
Adicionalmente, uma tabela de símbolo é gerada, responsável por armazenar palavras reservadas e identificadores;
tabela_lexema.csv é um arquivo utilizado para a visualização dos lexemas categorizados pelo analisador lexico;
tabela_simbolos.csv é um arquivo utilizado para a visualização da tabela de símbolos.

Utilizar os comandos a seguir para teste:

flex analisadorLexico.l               #GERA O ARQUIVO: lex.yy.c
gcc -o analisadorLexico lex.yy.c -ll  #COMPILA-O COM O NOME: analisadorLexico (testar com e sem a partícula -ll)
./arquivo                             #EXECUTA O ANALISADOR LEXICO PARA O ARQUIVO DE ENTRADA FORNECIDO
